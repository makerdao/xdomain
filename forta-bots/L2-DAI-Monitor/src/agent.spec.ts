import {
  FindingType,
  FindingSeverity,
  Finding,
  HandleTransaction,
  createTransactionEvent,
  ethers,
} from "forta-agent";

describe("L2 DAI Monitor tests", () => {
  it("do something", async () => {
    
  });
});
